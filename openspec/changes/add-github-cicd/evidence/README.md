# Evidence folder

Add screenshots here after the first successful GitHub Actions run on `main`:

| File | Content |
|------|---------|
| `workflow-run.png` | Actions → CI workflow, all jobs green |
| `test-report.png` | build-and-test → .NET Tests check or summary |
| `dockerhub-tags.png` | Docker Hub → `bookingsystemai-api` tags |

Record the pull command in `../DELIVERABLES.md`.
