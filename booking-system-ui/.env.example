# Local development (default API from launch profile http)
VITE_API_URL=http://localhost:5070

# Render static site: set at BUILD time (redeploy UI after change)
# Must match your real API host (e.g. bookingai-api, NOT bookingsystemai-api from Docker docs)
# VITE_API_URL=https://bookingai-api.onrender.com
